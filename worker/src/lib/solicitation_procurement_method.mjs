/**
 * Re-export solicitation procurement-method extraction from the shared site module
 * so ingest, notice_facts, and the browser UI share one implementation.
 */
export * from "../../../site/solicitation_procurement_method.mjs";

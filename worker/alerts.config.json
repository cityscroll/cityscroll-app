{
  "_comment": "Each watch is one saved slice of the City Record. The Worker shadow cron renders the real digest path at 10:00 UTC without delivery; the 13:00 UTC send cron re-runs each query, diffs against last delivery, and emails NEW notices to `email`. Leave `email` empty to skip sending. Nothing sends until ALERTS_LIVE is \"true\".",
  "watches": [
    {
      "id": "awards-1m",
      "label": "Contract awards over $1M",
      "email": "",
      "type": "awards",
      "min": 1000000,
      "limit": 25
    },
    {
      "id": "rivington",
      "label": "Anything mentioning Rivington",
      "email": "",
      "type": "search",
      "q": "Rivington",
      "limit": 20
    }
  ]
}

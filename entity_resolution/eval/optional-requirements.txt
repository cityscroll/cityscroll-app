# Optional, eval-only dependencies. Never install these in the site/Worker build.
splink>=4,<5
duckdb>=1,<2
dedupe>=3,<4
# GoldenMatch spike (bake-off contender only; never a production scorer).
goldenmatch>=3,<4
polars>=1,<2

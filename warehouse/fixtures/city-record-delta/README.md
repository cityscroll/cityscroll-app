# City Record delta fixture provenance

Every row, identifier, agency label, title, and date in this directory is a
synthetic fixture, hand-authored for the delta-export determinism test. The
values are not copied from City Record or any person-level record.

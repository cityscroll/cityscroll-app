# Warehouse fixtures

Synthetic sample rows for offline WH-01 proof only. Not measured city data.
Shape mirrors OCP Recent Contract Awards (`qyyg-4tf5`) columns used by the
product join, so ingest → parquet → DuckDB can be characterized without a
network call.

# Explicit remaining public strings that still use a listed internal-ish term.
# Prefer rewriting copy over adding lines. Format: key  OR  key:term
#
# Staffing / career guide product language intentionally distinguishes the
# precomputed DCAS schedule from live City Record searches.
staffing_upcoming_deck:precompute
staffing_notices_deck:precompute
staffing_exam_redirect_html:precompute
career_loading:precompute
career_noe_pending:precompute
career_load_failed:precompute
exam_spine_provenance_html:precompute
lifecycle_dollars_provenance_html:precompute
matter_spine_note:precompute
career_source_current:precompute
# API docs page names the Socrata SODA protocol on upstream dataset links.
api_p_upstream_html:SODA

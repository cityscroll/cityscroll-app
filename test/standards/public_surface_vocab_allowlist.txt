# Explicit remaining public strings that still use a listed internal-ish term.
# Prefer rewriting copy over adding lines. Format: key  OR  key:term
#
# Staffing / career guide product language previously used "precomputed" to
# distinguish the DCAS exam guide from live City Record searches; those strings
# were rewritten to plain reader language (overnight quality sweep). Keep this
# file for intentional exceptions only.
# API docs page names the Socrata SODA protocol on upstream dataset links.
api_p_upstream_html:SODA
# Calendar handoff copy uses the product's reader-facing scope term to identify
# the exact filtered event set being subscribed to.
calendar_this_scope:architecture jargon: scope

# stray_english_allowlist.txt — the explicit register of English string literals that
# do NOT (yet) route through the i18n layer. Every entry is a decision; the gate fails
# on NEW entries and on STALE ones, so this file can only shrink honestly.
# Sections queued for the 10-language wave are marked; delete entries as they get wired.

# ── NYC data-value constants — compared against dataset values, or abbreviation-map keys; never rendered as translated chrome
Administration for Children's Services
Mayor's Office of Contract Services
Agency Rules
Award
Board of Standards and Appeals
Changes in Personnel
City Planning
City Council
City University of New York
Contact
Days left
Design and Construction
Email
no fixed deadline (rolling)
Health and Hospitals Corporation
Health and Mental Hygiene
Housing Authority
Housing Preservation and Development
Information Technology and Telecommunications
Intent to Award
Intent to Negotiate
Method
Notice
Parks and Recreation
Procurement
Property Disposition
Public Hearings and Meetings
Public Hearings
Submit to
Taxi and Limousine Commission
Type
Youth and Community Development

# ── procurement category enum (DEEPLINK_CATEGORIES, index.html) — mirrors worker/src/lib/filter.mjs's
# CATEGORIES verbatim, dataset category_description values used only to clamp a deep-linked
# watch's category field; never rendered as translated chrome (category_description is already
# shown as raw English data elsewhere, same as agency_name/vendor_name). The slash-containing
# entries don't trip the word-list scan themselves (only their comma-joined neighbors did).
Goods and Services
Services (other than human services)

# ── SODA query fragments / column expressions
,event_date,street_address_1,section_name,additional_description_2,additional_description_3,other_info_2,other_info_3,printout_1,printout_2,printout_3,building_name,city,state,zip_code
agency_name, count(1) as n, sum(contract_amount) as t
count(1) as n, sum(contract_amount) as t
count(1) as n, sum(contract_amount) as total
selection_method_description, count(1) as n
sum(contract_amount) as t
title_description, count(1) as n, min(base_salary) as mn, max(base_salary) as mx, avg(base_salary) as avg
vendor_name, count(1) as n, sum(contract_amount) as t
vendor_name, count(1) as n, sum(contract_amount) as t, min(start_date) as first, max(start_date) as last

# ── Internal matching-logic constants — never rendered, only used for word-overlap scoring
the a an of for and to in on with by at services service contract contracts renewal option year years extension citywide fiscal
agency rules
land use
property disposition
public hearing
public hearings

# ── Key names, HTTP headers, ICS/calendar structure
Content-Type
DESCRIPTION:RFP response due tomorrow
DESCRIPTION:Tomorrow
Enter
METHOD:PUBLISH
RFP due:

# ── Outbound email bodies (letters of intent to city offices) — English by design
City Record notice:
Intent to respond:
Thank you,n[Your name — firm — phone]
This is a letter of intent in response to the following City of New York solicitation, as published in The City Record:nn
[One-line capability statement about your firm.]nn
nWe intend to respond and would appreciate confirmation of (1) how to obtain the full solicitation package and (2) whether responses are submitted via PASSPort or directly to your office.nn
nnVia CityScroll — NYC’s public record, linked.

# ── NL ask-layer: sample queries, device-parser placeholders, translate-chips (queued: language wave)
<span class="qchip">about <b>
<span class="qchip">due within <b>
<span class="qchip">in <b>
for example, HPD property sales, disposition hearings, DEP land
for example, buildings rules, rules open for comment
for example, education contracts over $200K due in 3 months, or awards over $1M
for example, paramedic roles, open competitive exams, or look up someone named Rodriguez
for example, hearings this week, landmarks, city council
for example, rezonings in Brooklyn, council district 33, or 79 Rivington
open RFPs
<span class="qchip">rezonings near <b>

# ── Entity / matter / investigation / shared views + digest mock shell (queued: language wave)
%</b> of the agency's award $ (12 mo)
%</b> of this agency's awards (last 12 mo, n=
project_id='
type_of_notice_description='Award'
type_of_notice_description='Solicitation'
">Search notices mentioning them →</a></div>
(agency median
(see notice)
</b> of
</div> <div class="note">Figures are what this agency has <b>published in the City Record</b> (2003→present for procurement) — registration and payment lag. Watch this agency for City Record notices across sections; the <a href="/agencies/
/">cross-category constellation</a> also surfaces staffing exam certifications when the publisher lists them.</div> </div></div>
</div><div class="lbl">award notice
</div><div class="lbl">contract awards<br>published</div></div> <div><div class="big">
</div><div class="lbl">first / latest<br>award published</div></div> </div>
</div><div class="lbl">open RFPs<br>right now</div></div> </div>
</div><div class="lbl">total awarded,<br>on record</div></div> <div><div class="big">
<div class="chain-h">Open solicitations</div><div class="timeline">
<div class="chain-h">Top vendors by awarded $ (click to pivot)</div><div class="ladder">
<div class="chain-h">Upcoming hearings &amp; events</div><div class="timeline">
<div class="emailmock"> <div class="ehead"><div class="efrom">CityScroll &lt;alerts@crol-list.org&gt; →
<div class="empty"><span class="loading"></span> building profile:
agency_name in(
<div class="empty">No awards on record for “
Due:
award to this vendor at this agency in 90 days
block/lot
city-owned property
fiscal_year=
forest management
in the custody
look up lookup someone somebody person people named name find search for a an the of about mr ms mrs role title
mortgage and note
notice/
task/what-will-change
#task/what-will-change
exam/
land/
owners are wanted
property clerk
public auction
request for proposal
residential property
text/calendar;charset=utf-8
the a an of in on for to and or with show me find list all near over under within new nyc city our your their about that this week month what can comment open competitive exams exam guide council district community board process stage hearing hearings auction disposition forecast contracts closing
the procurement officer
this vendor holds <b>
vendor_name in(
you@example.com
© OpenStreetMap © CARTO · lots © NYC MapPLUTO
⚑ non-competitive method:

# ── Map exploration (cs-geo-04) — route token; never rendered chrome
view=tax-lien

# Route-scoped stylesheet selectors and asset names; these are DOM plumbing, not chrome.
link[data-route-style="
property.css

# Stable board-decision destinations

Each admitted decision keeps a copyable destination derived from its candidate identity. Positional fragments that already shipped stay frozen to those original decisions.

Regenerate with:

```sh
python3 tools/capture_community_board_stable_decision_destinations.py
```

Images remain under `.artifacts/community-board-stable-decision-destinations/` and are not committed. The receipt is this manifest.

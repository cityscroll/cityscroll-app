{
  "schema": "cityscroll.architecture.observer_canaries.v1",
  "description": "Human-owned list of architecture-affecting surfaces. Register a surface here once; facts observer_coverage, the reconciliation workflow path-coverage check, and the watermark consume this list. Do not infer or auto-extend it.",
  "canaries": [
    {
      "id": "agency-constellation-model",
      "path": "site/agency_constellation_model.mjs",
      "why": "Agency constellation and graph producer. PR 1076 raised the PASSPort constellation ceiling on this surface; graph-row ceilings are invisible to the observer's closed source set."
    },
    {
      "id": "agency-search-producer",
      "path": "site/agency_search_producer.mjs",
      "why": "Agency SearchDocument producer. PR 1076 touched this file; it is absent from the observer's closed source set, so constellation/search-producer changes do not appear in facts."
    },
    {
      "id": "civic-geography-registry",
      "path": "site/civic_geography_registry.mjs",
      "why": "Typed civic-geography control plane. Layer types, classes, canonical IDs, source contracts, coverage rules, and declared uses are architecture-affecting even when legacy district routes remain unchanged."
    },
    {
      "id": "constellation-document-materializer",
      "path": "tools/build_agency_constellation_documents.mjs",
      "why": "Materializes the agency constellation lookup and documents. Constellation producers and their materializers sit outside the observer's closed source set."
    },
    {
      "id": "exams-eligibility",
      "path": "site/exams_surface.mjs",
      "why": "Exams public eligibility and Interest Area multi-select. PR 1056 changed fail-closed open_competitive admission; eligibility semantics are architecture-affecting and were outside the observer."
    },
    {
      "id": "keyword-search-index",
      "path": "tools/build_keyword_search_index.mjs",
      "why": "Keyword-index builder for typed SearchDocuments. PR 1058 changed it with production search; the observer does not parse tools/build_* builders."
    },
    {
      "id": "ontology-registry",
      "path": "ontology/registry.v0.json",
      "why": "Civic Graph catalog counts. Already in the observer's closed source set; registered so coverage of this surface is explicit rather than implicit."
    },
    {
      "id": "pages-edge-renderer",
      "path": "site/pages_edge.mjs",
      "why": "Pages-edge document renderer. Search, Browse, and Pages-edge sit outside the observer's closed source set."
    },
    {
      "id": "pages-edge-routes",
      "path": "site/_routes.json",
      "why": "Pages worker invocation boundary. A route family handled in the edge renderer but missing from this include list never reaches the worker."
    },
    {
      "id": "performance-observability-builder",
      "path": "tools/build_performance_observability.mjs",
      "why": "Deterministically projects the canonical RUM registry into browser classification, Worker validation, and private operator inventories."
    },
    {
      "id": "performance-observability-registry",
      "path": "architecture/performance-observability.v1.json",
      "why": "Canonical RUM metric, surface, component, lifecycle, and projection registry. Architecture facts and advisory coverage consume this source rather than maintaining another surface list."
    },
    {
      "id": "primary-document-materializer",
      "path": "tools/build_primary_documents.mjs",
      "why": "C4 materialization_tools cites this builder as the seam that writes compact site read models. The observer does not parse materializers."
    },
    {
      "id": "production-search",
      "path": "worker/src/search.mjs",
      "why": "Production Worker search federates collection families. Adding a family (PR 1058 Committees) does not change dispatch pathnames, so the route parser stays quiet."
    },
    {
      "id": "worker-bindings",
      "path": "worker/wrangler.toml",
      "why": "Production bindings, routes, and crons. The reconciler already checks this file against C4; registered so the already-observed surface stays in the canary set."
    }
  ]
}
